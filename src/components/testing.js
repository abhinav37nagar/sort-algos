const testing = ({ testfunc }) => {
  return (
    <div className="testing">
      <div className="test-btn" onClick={testfunc}>
        testing
      </div>
    </div>
  );
};

export default testing;
